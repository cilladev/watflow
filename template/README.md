# Workflow Name

Brief description of what this workflow does.

## Quick Start

```bash
# Install dependencies
pip install -r requirements.txt

# Set up environment
cp .env.example .env
# Edit .env with your API keys

# Run the workflow
python main.py
```

## What It Does

1. **Phase 1**: Description of first phase
2. **Phase 2**: Description of second phase
3. **Phase 3**: Description of third phase

## Configuration

Edit `config.yaml` to customize:

- AI model selection
- Parallel execution settings
- Workflow-specific settings

## Required API Keys

- `ANTHROPIC_API_KEY` - Claude AI (required)
- Add other required keys here

## Workflows

The `workflows/` directory contains Markdown SOPs that define what this automation does:

- `example.md` - Example workflow

## Tools

The `tools/` directory contains Python scripts that execute actions:

- `example_tool.py` - Example tool

---

**Part of the [WATFlow](https://github.com/jasonrobwebster/wf-automation) framework**
